name: Deploy Frontend to VPS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Verify deployment files
      run: |
        echo "Files to be deployed:"
        ls -la
        echo "Docker compose file:"
        cat docker-compose.yml

    - name: Copy files with SSH
      uses: easingthemes/ssh-deploy@main
      with:
        SSH_PRIVATE_KEY: ${{ secrets.VPS_SSH_KEY }}
        ARGS: "-rlgoDzvc -i"
        SOURCE: "./"
        REMOTE_HOST: ${{ secrets.VPS_HOST }}
        REMOTE_USER: ${{ secrets.VPS_SSH_USER }}
        TARGET: '/home/${{ secrets.VPS_SSH_USER }}/salao-frontend'
        EXCLUDE: "/dist/, /node_modules/, /.git/, /coverage/, /.vscode/, /.idea/"
        SCRIPT_AFTER: |
          # Build and start the frontend services
          cd /home/${{ secrets.VPS_SSH_USER }}/salao-frontend

          echo "Cleaning up unused Docker resources..."
          docker system prune --volumes -f

          # echo "Stopping existing containers..."
          # docker compose down || true

          echo "Building and starting frontend containers..."
          docker compose up -d --build

          echo "Waiting for containers to start..."
          sleep 10

          # Health check
          echo "Performing health check..."
          if curl -f http://app2.andrepombo.info > /dev/null 2>&1; then
            echo "✅ Frontend deployment successful! Application is responding."
          else
            echo "❌ Health check failed. Checking container logs..."
            docker compose logs
            exit 1
          fi

          echo "🚀 Frontend deployment completed successfully!"
